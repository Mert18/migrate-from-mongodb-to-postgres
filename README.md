# migrate-from-mongodb-to-postgres
Script for migrating mongodb databases to postgresql

`database.ini` format:
```
[postgresql]
host=<host-name>
database=<db-name>
user=<user-name>
password=<password>
``` 
